const {odd, even} = require('./var');

console.log(odd);



// {} = require() -> module.export


// import가 최신
// import {} from ''; -> export.default 파일 명

/**
 * global : 노드의 전역 객체  (브라우저 : window)
 * - global 생략 가능
 * - golbal 속성에 값을 대입하면 다른 파일에서도 사용 가능
 * 
 * 
 */